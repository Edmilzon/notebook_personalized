package com.example.notebook_personalized.ui.drawing

import android.app.Dialog
import android.content.Context

class DrawingToolsDialog(context: Context) : Dialog(context) {
    // Aquí irá la UI para seleccionar color, grosor, borrador, etc.
} 